---
description: RGW C Common 开发规范 - 编程规范和常见错误预防
globs: "**/*.c"
alwaysApply: true
---

# RGW C Common 编程规范与常见错误预防

本文档总结了 RGW C Common 项目中的编程规范和常见错误预防措施，适用于所有 C 代码编写。

---

## 1. API 命名规范

### 1.1 函数命名一致性

**规则 1.1.1: 使用统一的函数前缀**

- 所有 rgw_string 函数使用 `rgw_string_*` 前缀
- 所有 rgw_buffer 函数使用 `rgw_buffer_*` 前缀
- 所有 rgw_hex 函数使用 `rgw_hex_*` 前缀
- 所有 rgw_b64 函数使用 `rgw_b64_*` 前缀

**规则 1.1.2: 测试文件必须使用正确的 API 函数名**

---

## 2. 内存管理规范

### 2.1 内存分配与释放配对

**规则 2.1.1: 确保每个分配都有对应的释放**

- 使用 `rgw_c_alloc` 分配，必须用 `rgw_c_free` 释放
- 使用 `malloc` 分配，必须用 `free` 释放
- 不要混用不同的内存分配函数

**规则 2.1.2: 避免双重释放 (Double Free)**

- 手动释放后，避免再次通过析构函数释放
- 使用独立的内存管理，自己控制生命周期

**规则 2.1.3: 避免 use-after-free**

- 释放后将指针置为 NULL

**规则 2.1.4: 使用 AddressSanitizer 检测内存错误**

---

## 3. 数据结构规范

### 3.1 动态扩容要求

**规则 3.1.1: 容器必须支持动态扩容**

- 容量不足时进行扩容，策略通常为容量翻倍

**规则 3.1.2: 避免使用不支持扩容的底层数据结构**

- 不要依赖固定容量的数据结构

---

## 4. 宏定义规范

### 4.1 宏类型兼容性

**规则 4.1.1: 宏内部变量类型应使用最通用的类型**

- 使用 `int` 兼容所有整数类型

### 4.2 宏使用方式

**规则 4.2.1: 明确宏是语句还是表达式**

- 宏是语句，不能当作表达式使用
- 试图将宏的"结果"赋给变量会导致编译错误

---

## 5. 测试规范

### 5.1 测试代码质量

**规则 5.1.1: 测试必须验证正确行为而非仅验证编译通过**

- 测试函数返回值
- 测试边界条件
- 测试错误处理路径

**规则 5.1.2: 使用 AddressSanitizer 进行测试**

---

## 6. 代码审查检查清单

在提交代码前，必须检查以下内容：

- [ ] 是否有未释放的内存（每个 alloc 都有对应的 free）
- [ ] 是否有双重释放风险（手动释放后是否还会被调用释放）
- [ ] 容器是否支持动态扩容
- [ ] 宏的类型是否与调用方兼容
- [ ] 宏的使用方式是否正确
- [ ] 是否有未初始化的指针
- [ ] 边界条件是否正确处理（空容器、单个元素、容量边界）

---

## 7. 调试技巧

### 7.1 GDB 调试段错误

### 7.2 Valgrind 检测内存错误

### 7.3 AddressSanitizer

---

## 8. 项目特定规范

### 8.1 必须阅读的文档

在编写任何代码之前，**必须**完整阅读以下文档：

1. **`memory-bank/@architecture.md`** - 包含完整的数据库结构和架构设计
2. **`memory-bank/@cpp2c-document.md`** - C++ 转 C 的转换规范和指南
3. **`memory-bank/@implementation-plan.md`** - 实施计划

### 8.2 不要重复造轮子

- 如有需要，**必须**优先使用 `c_common` 文件夹下已经转换成 C 库的数据结构
- 现有的 C 封装包括：
  - `rgw_carray` - 动态数组 (std::vector)
  - `rgw_cstring` - 字符串 (std::string)
  - `rgw_cmap` - 有序 Map (std::map)
  - `rgw_cset` - 有序 Set (std::set)
  - `rgw_clist` - 双向链表 (std::list)
  - `rgw_cdeque` - 双端队列 (std::deque)
  - `rgw_cstack` - 栈 (std::stack)
  - `rgw_cqueue` - 队列 (std::queue)
  - `rgw_cpriority_queue` - 优先队列 (std::priority_queue)
  - `rgw_chash_map` - 哈希表 (std::unordered_map)
  - `rgw_coptional` - 可选类型 (std::optional)
  - `rgw_oop` - OOP 框架 (虚函数表、引用计数、智能指针)
  - `rgw_errors` - 错误处理框架
  - `rgw_ccommon` - 统一头文件 (包含所有容器)

### 8.3 编译和测试要求

- 每次代码修改后，**必须**运行编译和测试
- 确保所有测试通过后再结束任务
- 使用 `ReadLints` 检查代码质量

---

## 9. 字符串处理规范

### 9.1 字符函数安全使用

**规则 9.1.1: 使用 tolower/toupper 时必须转换为 unsigned char**

### 9.2 字符串转数值安全

**规则 9.2.1: 字符串转数值必须处理负数和溢出**

- 处理前导空格
- 检查负号
- 检查 LLONG_MAX 和 LLONG_MIN 边界

### 9.3 参数校验

**规则 9.3.1: 偏移量和大小参数必须检查负数**

---

## 10. 函数参数安全规范

### 10.1 空指针检查

**规则 10.1.1: 函数参数必须进行空指针检查**

- 对所有输入指针参数进行空指针检查
- 在解引用前验证指针有效性

**规则 10.1.2: 字符串比较前检查指针**

- 在调用 strcmp 等字符串函数前，检查指针是否为空

### 10.2 返回值安全

**规则 10.2.1: 返回内部指针时需谨慎**

- 返回内部指针可能导致 use-after-free
- 如需返回数据，应复制到输出参数

---

**文档版本**: 3.0
**生成日期**: 2026-03-18

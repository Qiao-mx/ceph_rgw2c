---
description: RGW C Common 开发规范 - 编程规范和常见错误预防
globs: "**/*.c"
alwaysApply: true
---

# RGW C Common 编程规范与常见错误预防

本文档总结了 RGW C Common 项目中的编程规范、常见错误及预防措施，适用于所有 C 代码编写。

## 0. 本次运行问题总结

### 0.1 测试结果

| 测试 | 状态 | 备注 |
|------|------|------|
| test_cstring | ✅ PASSED | 所有测试通过，执行时间 ~0.05s |
| test_cdeque | ✅ PASSED | 之前有双重释放问题，现已修复 |
| test_errors | ✅ PASSED | 宏类型问题已修复 |
| test_buffer | ✅ PASSED | 新创建，修复头文件后通过 |
| test_hex | ✅ PASSED | 新创建 |
| test_types | ✅ PASSED | 综合集成测试，修复 API 不匹配后通过 |
| test_b64 | ✅ PASSED | Base64 编解码功能，修复 4 个 bug 后通过（10/10 测试） |

### 0.2 已修复的问题

| 测试 | 问题类型 | 原因分析 | 修复状态 |
|------|----------|----------|----------|
| test_errors | 宏类型不匹配 | RGW_CHECK_MSG 宏内部使用 int，但 rgw_set_error 期望 rgw_error_code_t | ✅ 已修复 |
| test_types | API 不匹配 | 使用了错误的函数名 (rgw_cstring_*) 和参数类型 | ✅ 已修复 |
| test_buffer | 缺少头文件 | 未包含 rgw_cmemory.h，无法使用 rgw_c_alloc | ✅ 已修复 |
| test_b64 | 参数验证过严 | rgw_b64_encode_binary 强制要求 output_len 参数不能为 NULL | ✅ 已修复 |
| test_b64 | 编码填充计算错误 | Base64 编码时填充字符数量计算逻辑错误 | ✅ 已修复 |
| test_b64 | 解码填充处理错误 | Base64 解码时将填充字符'='当作有效数据处理（值为 -1） | ✅ 已修复 |
| test_b64 | 长度计算错误 | 解码时使用已修改的 input_len 而非原始长度计算 | ✅ 已修复 |

### 0.3 经验教训

1. **内存管理**：必须明确每个内存分配的生命周期，避免双重释放
2. **宏定义**：宏内部变量类型应与被调用函数期望的类型保持一致
3. **测试验证**：测试必须验证正确行为而非仅验证编译通过
4. **字符处理**：C 标准库的字符函数（tolower, toupper 等）需要先将 char 转换为 unsigned char，避免符号扩展问题
5. **数值边界**：字符串转数值函数必须处理负数和边界溢出情况
6. **参数校验**：函数参数必须检查负数情况
7. **可选参数**：函数参数设计时应考虑可选性，避免强制要求不必要的参数
8. **填充处理**：编解码算法中的填充字符必须特殊处理，不能当作普通数据
9. **状态修改**：在计算过程中修改输入状态时，应保存原始值用于后续计算

---

## 1. API 命名规范

### 1.1 函数命名一致性

**规则 1.1.1: 使用统一的函数前缀**

- 所有 rgw_string 函数使用 `rgw_string_*` 前缀
- 所有 rgw_buffer 函数使用 `rgw_buffer_*` 前缀
- 所有 rgw_hex 函数使用 `rgw_hex_*` 前缀
- 所有 rgw_b64 函数使用 `rgw_b64_*` 前缀

**规则 1.1.2: 测试文件必须使用正确的 API 函数名**

错误示例:
```c
// 错误：使用了不存在的函数
rgw_cstring_t* str = rgw_cstring_create("hello");  // 不存在
char* encoded = NULL;
rgw_b64_encode_str(original, &encoded);  // 不存在
```

正确做法:
```c
// 正确：使用正确的 API
rgw_string_t* str = rgw_string_create("hello");
char* encoded = NULL;
rgw_b64_encode((const uint8_t*)original, strlen(original), &encoded);
```

---

## 2. 头文件包含规范

### 1.1 内存分配与释放配对

**规则 1.1.1: 确保每个分配都有对应的释放**

- 使用 `rgw_c_alloc` 分配，必须用 `rgw_c_free` 释放
- 使用 `malloc` 分配，必须用 `free` 释放
- 不要混用不同的内存分配函数

**规则 1.1.2: 避免双重释放 (Double Free)**

错误示例:
```c
// 手动释放第一个元素
rgw_c_free(elem->data);
// 错误：utringbuffer_clear 会再次调用 dtor 释放
utringbuffer_clear(buffer);
```

正确做法:
```c
// 方案 A: 直接手动清理整个缓冲区
for (size_t i = 0; i < len; i++) {
    rgw_c_free(buffer[i].data);
}
buffer->i = 0;
buffer->f = 0;

// 方案 B: 使用独立的内存管理，自己控制生命周期
// 不依赖第三方的 dtor 回调
```

### 1.2 避免使用-after-free

**规则 1.2.1: 释放后将指针置为 NULL**

错误示例:
```c
rgw_c_free(elem->data);
// 后面可能还会访问 elem->data
```

正确做法:
```c
rgw_c_free(elem->data);
elem->data = NULL;
```

### 1.3 内存泄漏检测

**规则 1.3.1: 使用 AddressSanitizer 检测内存错误**

```bash
# 编译时启用 AddressSanitizer
gcc -fsanitize=address -g test.c -o test
./test

# 可检测: 内存泄漏、双重释放、使用-after-free 等
```

---

## 2. 数据结构规范

### 2.1 动态扩容要求

**规则 2.1.1: 容器必须支持动态扩容**

错误示例:
```c
#define DEFAULT_CAPACITY 16
// 环状缓冲区满了就覆盖旧数据
```

正确做法:
```c
int rgw_deque_push_back(deque, value) {
    if (deque->size >= deque->capacity) {
        // 扩容策略：容量翻倍
        new_capacity = deque->capacity * 2;
        resize(deque, new_capacity);
    }
    // ...
}
```

**规则 2.1.2: 避免使用不支持扩容的底层数据结构**

- 不要依赖 utringbuffer 的固定容量特性
- 实现自己的动态数组或使用 utarray (支持扩容)

---

## 3. 宏定义规范

### 3.1 宏类型兼容性

**规则 3.1.1: 宏内部变量类型应使用最通用的类型**

错误示例:
```c
#define RGW_CHECK_MSG(expr, msg) \
    do { \
        rgw_error_code_t _rc = (expr); \  // 可能是 int
        if (_rc != RGW_OK) { ... }
    } while(0)
```

正确做法:
```c
#define RGW_CHECK_MSG(expr, msg) \
    do { \
        int _rc = (expr); \  // 使用 int 兼容所有整数类型
        if (_rc != RGW_OK) { ... }
    } while(0)
```

### 3.2 宏使用方式

**规则 3.2.1: 明确宏是语句还是表达式**

- 宏是语句（执行后可能 return），不能当作表达式使用
- 试图将宏的"结果"赋给变量会导致编译错误或逻辑错误

错误示例:
```c
int rc = RGW_CHECK_MSG(some_func(), "error");
// 宏内部 return，无法赋值给 rc
```

正确做法:
```c
// 方式 1: 测试宏的逻辑而非直接使用宏
int val = some_func();
if (val != RGW_OK) {
    rgw_set_error(val, "error", __FILE__, __LINE__);
}

// 方式 2: 如果宏确实返回值，不要在表达式中使用
RGW_CHECK_MSG(some_func(), "error");
// 后续代码继续执行
```

---

## 4. 测试规范

### 4.1 测试代码质量

**规则 4.1.1: 测试必须验证正确行为而非仅验证编译通过**

- 测试函数返回值
- 测试边界条件
- 测试错误处理路径

**规则 4.1.2: 使用 AddressSanitizer 进行测试**

```bash
# 编译测试程序
gcc -fsanitize=address -g test.c -o test
# 运行测试
./test
```

---

## 5. 代码审查检查清单

在提交代码前，必须检查以下内容：

- [ ] 是否有未释放的内存（每个 alloc 都有对应的 free）
- [ ] 是否有双重释放风险（手动释放后是否还会被调用释放）
- [ ] 容器是否支持动态扩容
- [ ] 宏的类型是否与调用方兼容
- [ ] 宏的使用方式是否正确
- [ ] 是否有未初始化的指针
- [ ] 边界条件是否正确处理（空容器、单个元素、容量边界）
- [ ] **新增**：函数参数验证是否考虑了可选参数（不要强制要求不必要的参数）
- [ ] **新增**：编解码算法是否正确处理填充字符（不能当作普通数据）
- [ ] **新增**：在修改变量前是否保存了需要的原始值
- [ ] **新增**：是否为编解码算法编写了完整的测试用例（包括所有填充场景）

---

## 6. 调试技巧

### 6.1 GDB 调试段错误

```bash
gdb -batch -ex "run" -ex "bt" ./test_program
```

### 6.2 Valgrind 检测内存错误

```bash
valgrind --leak-check=full --track-origins=yes ./test_program
```

### 6.3 AddressSanitizer

```bash
gcc -fsanitize=address -g test.c -o test
./test
```

---

## 7. 项目特定规范

### 7.1 必须阅读的文档

在编写任何代码之前，**必须**完整阅读以下文档：

1. **`memory-bank/@architecture.md`** - 包含完整的数据库结构和架构设计
2. **`memory-bank/@cpp2c-document.md`** - C++ 转 C 的转换规范和指南
3. **`memory-bank/@implementation-plan.md`** - 实施计划

### 7.2 不要重复造轮子

- 如有需要，**必须**优先使用 `c_common` 文件夹下已经转换成 C 库的数据结构
- 现有的 C 封装包括：
  - `rgw_carray` - 动态数组 (std::vector)
  - `rgw_cstring` - 字符串 (std::string)
  - `rgw_cmap` - 有序 Map (std::map)
  - `rgw_cset` - 有序 Set (std::set)
  - `rgw_clist` - 双向链表 (std::list)
  - `rgw_cdeque` - 双端队列 (std::deque)
  - `rgw_cstack` - 栈 (std::stack)
  - `rgw_cqueue` - 队列 (std::queue)
  - `rgw_cpriority_queue` - 优先队列 (std::priority_queue)
  - `rgw_chash_map` - 哈希表 (std::unordered_map)
  - `rgw_coptional` - 可选类型 (std::optional)
  - `rgw_oop` - OOP 框架 (虚函数表、引用计数、智能指针)
  - `rgw_errors` - 错误处理框架
  - `rgw_ccommon` - 统一头文件 (包含所有容器)

### 7.3 编译和测试要求

- 每次代码修改后，**必须**运行编译和测试：
  ```bash
  cd build && cmake .. && make -j4
  ctest --output-on-failure
  ```
- 确保所有测试通过后再结束任务
- 使用 `ReadLints` 检查代码质量

---

## 8. 字符串处理规范

### 8.1 字符函数安全使用

**规则 8.1.1: 使用 tolower/toupper 时必须转换为 unsigned char**

错误示例:
```c
// 错误：char 可能为负数，导致 tolower 未定义行为
tolower(*p) == tolower(*s)
```

正确做法:
```c
// 正确：先将 char 转换为 unsigned char
tolower((unsigned char)*p) == tolower((unsigned char)*s)
```

### 8.2 字符串转数值安全

**规则 8.2.1: 字符串转数值必须处理负数和溢出**

错误示例:
```c
long long result = strtoll(s, &end, 10);
if (result == LLONG_MAX && errno == ERANGE) {
    return -ERANGE;
}
```

正确做法:
```c
// 处理前导空格
while (*s == ' ' || *s == '\t') s++;

// 检查负号
int neg = 0;
if (*s == '-') { neg = 1; s++; }

long long result = strtoll(s, &end, 10);
if (result == LLONG_MAX && errno == ERANGE) return -ERANGE;
if (result == LLONG_MIN && errno == ERANGE) return -ERANGE;
```

### 8.3 参数校验

**规则 8.3.1: 偏移量和大小参数必须检查负数**

错误示例:
```c
// 错误：未检查负数参数
if ((size_t)ofs >= s1_len) return -1;
```

正确做法:
```c
// 正确：先检查负数
if (ofs < 0 || size < 0) return -1;
if ((size_t)ofs >= s1_len) return -1;
```

---

## 9. 编解码算法规范（Base64 经验总结）

### 9.1 可选参数设计

**规则 9.1.1: 函数参数应设计为可选的，避免强制要求不必要的参数**

问题案例（rgw_b64_encode_binary）:
```c
// 错误：强制要求 output_len 不能为 NULL
int rgw_b64_encode_binary(const uint8_t *input, size_t input_len,
                          uint8_t **output, size_t *output_len)
{
    if (!input || !output || !output_len) {  // 错误！output_len 可能不需要
        return RGW_B64_ERR_INVALID;
    }
    // ...
    *output_len = encoded_len;  // 总是写入，即使调用方不需要
}
```

正确做法:
```c
// 正确：output_len 是可选参数
int rgw_b64_encode_binary(const uint8_t *input, size_t input_len,
                          uint8_t **output, size_t *output_len)
{
    if (!input || !output) {  // 只检查必需参数
        return RGW_B64_ERR_INVALID;
    }
    // ...
    if (output_len) {  // 只在需要时写入
        *output_len = encoded_len;
    }
}
```

**原因分析**：
- 高层封装函数（如 `rgw_b64_encode`）可能不需要长度参数
- 强制要求会导致 API 使用困难或返回错误

**预防措施**：
- 设计 API 时考虑不同使用场景
- 对于可选参数，使用指针并在调用前检查是否为 NULL
- 在文档中明确标注哪些参数是可选的

---

### 9.2 填充字符处理

**规则 9.2.1: 编解码算法中的填充字符必须特殊处理，不能当作普通数据**

#### 9.2.1-A 编码时的填充计算

错误案例（Base64 编码）:
```c
// 错误：填充数量计算逻辑混乱
size_t padding = input_len % 3;
if (padding > 0) {
    for (size_t p = padding; p < 3; p++) {  // 循环次数错误
        out[encoded_len - 1 - p] = '=';
    }
}
```

问题分析：
- 当 `input_len = 5` 时，`padding = 2`
- 循环 `for (p = 2; p < 3; p++)` 只执行 1 次（p=2）
- 但实际上应该添加 1 个填充字符（逻辑碰巧正确，但计算方式错误）

正确做法:
```c
// 正确：清晰计算需要的填充数量
size_t mod = input_len % 3;
if (mod > 0) {
    /* mod=1 时需要 2 个填充，mod=2 时需要 1 个填充 */
    size_t pad_count = 3 - mod;  // 明确计算填充数量
    for (size_t p = 0; p < pad_count; p++) {
        out[encoded_len - 1 - p] = '=';
    }
}
```

#### 9.2.1-B 解码时的填充处理

错误案例（Base64 解码）:
```c
// 错误：将填充字符'='当作有效数据处理
while (i < input_len) {
    int sextet_a = b64_decode_table[input[i++]];
    int sextet_b = b64_decode_table[input[i++]];
    int sextet_c = b64_decode_table[input[i++]];  // 可能是'='，返回 -1
    int sextet_d = b64_decode_table[input[i++]];  // 可能是'='，返回 -1
    
    uint32_t triple = (sextet_a << 18) + (sextet_b << 12) +
                      (sextet_c << 6) + sextet_d;  // -1 参与运算，污染结果
    // ...
}
```

问题影响：
- 填充字符 '=' 在解码表中值为 -1（0xFFFFFFFF）
- 参与位运算后会污染最终结果
- 例如："SGVsbG8=" 解码为 "Helln" (0x6E) 而不是 "Hello" (0x6F)

正确做法:
```c
// 正确：遇到填充字符时设为 0
while (i < input_len) {
    int sextet_a = b64_decode_table[input[i++]];
    int sextet_b = b64_decode_table[input[i++]];
    /* 读取 sextet_c 和 sextet_d，如果是填充则设为 0 */
    int sextet_c = (i < input_len && input[i] != '=') ? b64_decode_table[input[i]] : 0;
    i++;
    int sextet_d = (i < input_len && input[i] != '=') ? b64_decode_table[input[i]] : 0;
    i++;
    
    // ... 继续处理
}
```

**原因分析**：
- 编解码算法通常有填充机制来对齐数据
- 填充字符不是有效数据，不能参与正常的数据转换
- 解码表通常将无效字符标记为 -1，但 -1 参与运算会产生意外结果

**预防措施**：
- 在处理填充字符前，显式检查并特殊处理
- 使用条件判断将填充字符转换为中性值（如 0）
- 添加单元测试覆盖所有填充场景（0 个、1 个、2 个填充字符）

---

### 9.3 状态修改与原始值保存

**规则 9.3.1: 在计算过程中修改输入状态时，应保存原始值用于后续计算**

错误案例（Base64 解码长度计算）:
```c
// 错误：使用已修改的 input_len 计算解码长度
size_t padding = 0;
while (input_len > 0 && input[input_len - 1] == '=') {
    padding++;
    input_len--;  // input_len 被修改
}

// ...
size_t decoded_len = (input_len / 4) * 3 - padding;  // 使用了错误的 input_len!
```

问题分析：
- 为了移除填充字符，`input_len` 被递减
- 后续计算解码长度时使用了已经被修改的 `input_len`
- 导致解码长度计算错误

例如："SGVsbG8=" (len=8)
- 移除 1 个 '=' 后，`input_len = 7`, `padding = 1`
- 错误计算：`(7 / 4) * 3 - 1 = 1 * 3 - 1 = 2` （应该是 5）

正确做法:
```c
// 正确：保存原始长度用于后续计算
/* 保存原始长度用于后续计算 */
size_t original_input_len = input_len;

/* 移除末尾的填充字符 '=' 并计算实际长度 */
size_t padding = 0;
while (input_len > 0 && input[input_len - 1] == '=') {
    padding++;
    input_len--;
}

// ...
/* 计算解码后的长度：使用原始长度计算 */
size_t decoded_len = (original_input_len / 4) * 3 - padding;
```

**原因分析**：
- 在算法执行过程中经常需要修改临时变量
- 但某些计算步骤可能需要原始值
- 忘记保存原始值会导致计算错误

**预防措施**：
- 在修改变量前，考虑后续是否需要原始值
- 如需保留，使用有意义的变量名（如 `original_input_len`）
- 在代码注释中说明为什么要保存原始值

---

### 9.4 编解码测试用例设计

**规则 9.4.1: 编解码算法必须测试以下场景**

1. **基本功能测试**
   - 编码：普通字符串 → Base64
   - 解码：Base64 → 原始字符串
   - 往返：编码后再解码，验证一致性

2. **边界条件测试**
   - 空字符串
   - 单个字节
   - 刚好填满一个块（3 字节）
   - 需要不同数量填充的场景（余数 0、1、2）

3. **特殊数据测试**
   - 二进制数据（包含 0x00）
   - 特殊字符（!@#$%^&*()）
   - 带空白字符的 Base64（空格、换行、制表符）

4. **错误处理测试**
   - NULL 指针参数
   - 无效的 Base64 字符串（长度不是 4 的倍数）
   - 包含非法字符的 Base64

测试用例示例:
```c
// 测试不同填充场景
TEST(test_b64_various_padding)
{
    // 无填充（3 的倍数）
    const char *input1 = "ABC";  // 3 bytes
    // 期望输出："QUJD" (无'=')
    
    // 1 个填充
    const char *input2 = "AB";  // 2 bytes
    // 期望输出："QUI=" (1 个'=')
    
    // 2 个填充
    const char *input3 = "A";  // 1 byte
    // 期望输出："QQ==" (2 个'=')
}
```

---

**文档版本**: 2.2
**生成日期**: 2026-03-17
**基于错误**: test_cdeque (段错误/双重释放), test_errors (宏类型不匹配), rgw_string_c (字符转换/数值边界/参数校验), **rgw_b64 (可选参数设计/填充处理/状态保存)**
